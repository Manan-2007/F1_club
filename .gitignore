# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.vercel

# Env — never commit credentials (.env.example IS committed)
.env.local
.env.*.local
*.local

# Firebase service account (used only by scripts/seed.js)
serviceAccountKey.json

# Neon data export — contains applicant emails, keep private
scripts/neon-export.json

# Leva debug panel (removed in Phase 5 — just in case)
.leva/

# OS
.DS_Store
Thumbs.db

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.vscode/settings.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
